<ul>
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article_path(article) %></li>
  <% end %>
</ul>

<%= link_to "Create an article", articles_new_path, class: "btn btn-primary btn-lg btn-block" %>
